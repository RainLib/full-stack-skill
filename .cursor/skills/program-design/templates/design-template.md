# 程序设计文档

## 1. 技术栈回顾

- 开发范围：全栈 / 仅前端 / 仅后端
- 关键技术：前端框架、后端框架、数据库等（从选型清单摘要）

## 2. 模块与分层

### 前端（若有）

- **页面/路由**：（列表）
- **组件**：通用组件、业务组件
- **状态/服务**：与后端或 mock 的对接方式

### 后端（若有）

- **API 层**：控制器/路由、请求校验
- **业务层**：服务、领域逻辑
- **数据层**：仓储/DAO、实体与持久化

### 数据模型（核心实体）

- 实体名、主要字段、关系（简要）

## 3. 关键流程

（用 1～3 个核心流程说明调用顺序与接口）

- **流程名**：步骤1 → 步骤2 → …（涉及的前后端接口/模块）

## 4. 工程目录结构

```
项目根目录/
├── （前端目录，若全栈/仅前端）
│   ├── src/
│   │   ├── pages/
│   │   ├── components/
│   │   └── ...
│   └── ...
├── （后端目录，若全栈/仅后端）
│   ├── src/
│   │   ├── controller/
│   │   ├── service/
│   │   └── ...
│   └── ...
└── （其他：脚本、配置等）
```

（按实际选型增删层级与命名）
